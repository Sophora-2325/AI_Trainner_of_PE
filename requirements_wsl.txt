# AI 健身教练 — WSL2 端依赖
# 安装方式 (WSL2中):
#   conda create -n fitness_coach python=3.10
#   conda activate fitness_coach
#   conda install -c opensim-org opensim
#   pip install -r requirements_wsl.txt

# 数值计算
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# 深度学习训练
torch>=2.0.0,<3.0.0
# torchvision  # 如需数据增强

# 配置
pyyaml>=6.0

# OpenSim — 必须通过 Conda 安装
# conda install -c opensim-org opensim

# 开发工具
# pytest>=7.0.0
